#include <stdio.h>

int main (){
    int num;
    int suma_impares = 0;

    prinft ("igngrese un numero");
    scanf("%d", &num)

    for (int i = 1; i <= n; i += 2){
        suma_impares += i; 
    }

    printf("La suma de los numeros impares %d es: %d\n", n, suma_impares);

    return 0;

}   